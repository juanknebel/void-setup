# Backups generated by setup-dotfiles.sh when overwriting an existing config.
*.bak.*

# Claude Code local settings (per machine/user, do NOT version).
.claude/settings.local.json

# Editor junk.
*.swp
*.swo
*~

# Python caches (in case tooling is added later).
__pycache__/
